# Local MCP config if you add a PAT (OAuth HTTP config above is safe to commit)
.cursor/mcp.local.json

# Environment secrets
.env
.env.*
!.env.example

# Local DB migration dumps (may contain user data)
supabase/migration-dump/

# Gemini batch job artefacts (regenerate locally)
batch-output/
batch-work/
